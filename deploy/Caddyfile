# Caddy reverse proxy for the GEO API.
# API_DOMAIN is injected from .env (e.g. api.getbrandradar.com). Once your DNS
# A-record points this name at the server, Caddy provisions a Let's Encrypt
# cert automatically — no manual certbot.
{$API_DOMAIN} {
	encode gzip
	reverse_proxy api-web:3000
}
